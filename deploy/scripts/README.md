# deploy/scripts

Utility scripts for building, deploying, and operating Burncloud.

| Script | Description |
|--------|-------------|
| _(none yet)_ | Add operational scripts here (e.g. `backup-db.sh`, `migrate.sh`, `seed-dev.sh`) |

## Usage

```bash
# From repo root
bash deploy/scripts/<script-name>.sh
```

Scripts should be idempotent and safe to run on a fresh environment.
