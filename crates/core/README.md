# burncloud-core

核心配置和模型管理。提供 ConfigManager 和 ModelManager,作为应用层的配置中枢。

## 关键类型

| 类型 | 说明 |
|------|------|
| `ConfigManager` | 全局配置管理 |
| `ModelManager` | 模型管理 |

## 依赖

- `burncloud-common` — 共享类型
- `tokio` — 异步运行时
