# burncloud-client

Dioxus-based GUI 客户端，支持 LiveView Web 模式。

## 🧅 第一层：系统中的位置

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           BurnCloud 系统架构                                 │
│                                                                              │
│   用户请求                                                                   │
│      │                                                                       │
│      ▼                                                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                         🌐 入口层                                    │    │
│  │                         burncloud                                   │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│      │                                                                       │
│      ▼                                                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐              │    │
│  │  │   Server    │    │   Router    │    │   Client    │              │    │
│  │  │  (控制平面)  │    │  (数据平面)  │    │  (用户界面)  │◄─★ 当前    │    │
│  │  └─────────────┘    └─────────────┘    └─────────────┘              │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

Client 的职责:
├── 提供用户界面 (GUI / Web)
├── Dioxus 组件渲染
├── LiveView 实时更新
├── API 调用和状态管理
└── 用户交互处理
```

## 🧅 第二层：Client 整体架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Client 整体架构                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                           Router (Dioxus)                              │  │
│  │                                                                        │  │
│  │   /             ──►  home.rs        # 首页                            │  │
│  │   /login        ──►  login.rs       # 登录                            │  │
│  │   /dashboard    ──►  dashboard.rs   # 仪表盘                          │  │
│  │   /models       ──►  models.rs      # 模型管理                        │  │
│  │   /monitor      ──►  monitor.rs     # 系统监控                        │  │
│  │   /logs         ──►  logs.rs        # 日志查询                        │  │
│  │   /settings     ──►  settings.rs    # 设置                            │  │
│  │   /billing      ──►  billing.rs     # 账单                            │  │
│  │   /playground   ──►  playground.rs  # API 测试                        │  │
│  │   /api          ──►  api.rs         # API 管理                        │  │
│  │   /connect      ──►  connect.rs     # 连接设置                        │  │
│  │   /deploy       ──►  deploy.rs      # 部署                            │  │
│  │                                                                        │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                      │                                       │
│                                      ▼                                       │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                         components/                                    │  │
│  │  ┌────────────────┐  ┌────────────────┐                              │  │
│  │  │    layout.rs   │  │  guest_layout  │                              │  │
│  │  │                │  │     .rs        │                              │  │
│  │  │  主布局组件    │  │  访客布局      │                              │  │
│  │  └────────────────┘  └────────────────┘                              │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                          子 Crates                                     │  │
│  │                                                                        │  │
│  │  client-dashboard  client-monitor  client-users  client-finance       │  │
│  │  client-access     client-log      client-connect client-playground   │  │
│  │  client-register   client-settings client-models  client-deploy       │  │
│  │  client-shared     client-tray     client-api                         │  │
│  │                                                                        │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第三层：源码结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            client/ 结构                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  crates/client/                                                              │
│  ├── src/                                                                    │
│  │   ├── lib.rs              # 入口，liveview_router()                      │
│  │   ├── main.rs             # GUI 启动入口                                 │
│  │   ├── app.rs              # Dioxus App 组件                              │
│  │   ├── tests.rs            # 测试                                         │
│  │   │                                                                      │
│  │   ├── pages/              # 页面组件                                     │
│  │   │   ├── mod.rs                                                          │
│  │   │   ├── home.rs          # 首页                                        │
│  │   │   ├── login.rs         # 登录                                        │
│  │   │   ├── dashboard.rs     # 仪表盘                                      │
│  │   │   ├── models.rs        # 模型管理                                    │
│  │   │   ├── monitor.rs       # 系统监控                                    │
│  │   │   ├── logs.rs          # 日志查询                                    │
│  │   │   ├── settings.rs      # 设置                                        │
│  │   │   ├── billing.rs       # 账单                                        │
│  │   │   ├── playground.rs    # API 测试                                    │
│  │   │   ├── api.rs           # API 管理                                    │
│  │   │   ├── connect.rs       # 连接设置                                    │
│  │   │   ├── deploy.rs        # 部署                                        │
│  │   │   ├── user.rs          # 用户管理                                    │
│  │   │   └── not_found.rs     # 404 页面                                    │
│  │   │                                                                      │
│  │   └── components/         # 可复用组件                                   │
│  │       ├── mod.rs                                                          │
│  │       ├── layout.rs        # 主布局                                      │
│  │       └── guest_layout.rs  # 访客布局                                    │
│  │                                                                          │
│  └── crates/                # 子 Crates                                     │
│      ├── client-shared/      # 共享组件和服务                               │
│      ├── client-monitor/     # 监控功能                                     │
│      ├── client-api/         # API 客户端                                   │
│      ├── client-deploy/      # 部署功能                                     │
│      ├── client-register/    # 注册功能                                     │
│      └── client-models/      # 模型功能                                     │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第四层：路由结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              路由结构                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  liveview_router(db: Arc<Database>) → Router<DioxusRouterContext>           │
│                                                                              │
│  路由定义:                                                                   │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  Route::new()                                                          │  │
│  │    .route("/", home::Home)                                             │  │
│  │    .route("/login", login::Login)                                      │  │
│  │    .route("/dashboard", dashboard::Dashboard)                          │  │
│  │    .route("/models", models::Models)                                   │  │
│  │    .route("/monitor", monitor::Monitor)                                │  │
│  │    .route("/logs", logs::Logs)                                         │  │
│  │    .route("/settings", settings::Settings)                             │  │
│  │    .route("/billing", billing::Billing)                                │  │
│  │    .route("/playground", playground::Playground)                       │  │
│  │    .route("/api", api::Api)                                            │  │
│  │    .route("/connect", connect::Connect)                                │  │
│  │    .route("/deploy", deploy::Deploy)                                   │  │
│  │    .route("/user", user::User)                                         │  │
│  │    .fallback(not_found::NotFound)                                      │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  布局嵌套:                                                                   │
│                                                                              │
│    Guest Routes (未登录)                                                     │
│    ┌─────────────────────────────────────────────────────────────────────┐  │
│    │  GuestLayout                                                         │  │
│    │  ├── /login                                                          │  │
│    │  └── /register                                                       │  │
│    └─────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│    Protected Routes (已登录)                                                 │
│    ┌─────────────────────────────────────────────────────────────────────┐  │
│    │  Layout (侧边栏 + 标题栏)                                            │  │
│    │  ├── /dashboard                                                      │  │
│    │  ├── /models                                                         │  │
│    │  ├── /monitor                                                        │  │
│    │  ├── /logs                                                           │  │
│    │  ├── /settings                                                       │  │
│    │  ├── /billing                                                        │  │
│    │  ├── /playground                                                     │  │
│    │  ├── /api                                                            │  │
│    │  ├── /connect                                                        │  │
│    │  ├── /deploy                                                         │  │
│    │  └── /user                                                           │  │
│    └─────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第五层：页面组件详解

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            页面组件                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  home.rs - 首页                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  功能: 欢迎信息 / 快速入口 / 系统状态概览                              │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  login.rs - 登录页                                                           │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  功能: 用户名密码输入 / 表单验证 / 登录 API 调用 / Token 存储          │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  dashboard.rs - 仪表盘                                                       │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  功能: 今日请求数 / Token 使用量 / 费用统计 / 趋势图表                 │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  models.rs - 模型管理                                                        │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  功能: 模型列表 / 价格查看 / 能力配置 / 路由设置                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  monitor.rs - 系统监控                                                       │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  功能: CPU/内存/磁盘使用率 / 实时刷新                                  │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  logs.rs - 日志查询                                                          │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  功能: 日志列表 / 时间筛选 / 状态码筛选 / 详情查看                     │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  billing.rs - 账单                                                           │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  功能: 余额显示 (USD/CNY) / 充值记录 / 消费明细 / 汇率显示             │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  playground.rs - API 测试                                                    │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  功能: 请求编辑器 / 响应查看 / 流式输出 / 历史记录                     │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  user.rs - 用户管理                                                          │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  功能: 用户列表 / 创建用户 / 编辑用户 / 余额调整 / Token 管理          │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第六层：client-shared - 共享模块

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          client-shared                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  src/                                                                        │
│  ├── lib.rs                  # 入口                                         │
│  ├── main.rs                 # 独立入口                                     │
│  ├── auth_context.rs         # 认证上下文                                   │
│  ├── i18n.rs                 # 国际化                                       │
│  ├── styles.rs               # 样式定义                                     │
│  ├── api_client.rs           # API 客户端                                   │
│  │                                                                          │
│  ├── components/             # 共享组件                                     │
│  │   ├── layout.rs            # 主布局                                      │
│  │   ├── sidebar.rs           # 侧边栏                                      │
│  │   ├── title_bar.rs         # 标题栏                                      │
│  │   ├── logo.rs              # Logo                                        │
│  │   ├── bc_button.rs         # 按钮组件                                    │
│  │   ├── bc_input.rs          # 输入组件                                    │
│  │   ├── bc_card.rs           # 卡片组件                                    │
│  │   ├── bc_table.rs          # 表格组件                                    │
│  │   ├── bc_badge.rs          # 徽章组件                                    │
│  │   ├── bc_modal.rs          # 模态框                                      │
│  │   ├── glass_card.rs        # 玻璃卡片                                    │
│  │   ├── aurora_background.rs # 极光背景                                    │
│  │   └── toast.rs             # 消息提示                                    │
│  │                                                                          │
│  ├── services/               # API 服务                                     │
│  │   ├── auth_service.rs      # 认证服务                                    │
│  │   ├── user_service.rs      # 用户服务                                    │
│  │   ├── channel_service.rs   # 通道服务                                    │
│  │   ├── token_service.rs     # Token 服务                                  │
│  │   ├── log_service.rs       # 日志服务                                    │
│  │   └── monitor_service.rs   # 监控服务                                    │
│  │                                                                          │
│  └── utils/                  # 工具函数                                     │
│      ├── storage.rs           # 本地存储                                    │
│      └── validation.rs        # 表单验证                                    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第七层：共享组件详解

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            共享组件                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  布局组件:                                                                   │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  Layout (主布局)                                                       │  │
│  │  ┌─────────────────────────────────────────────────────────────────┐  │  │
│  │  │  ┌──────────┐  ┌────────────────────────────────────────────┐  │  │  │
│  │  │  │ Sidebar  │  │ Main Content                               │  │  │  │
│  │  │  │          │  │                                            │  │  │  │
│  │  │  │ • 首页   │  │  ┌──────────────────────────────────────┐ │  │  │  │
│  │  │  │ • 仪表盘 │  │  │ Title Bar                            │ │  │  │  │
│  │  │  │ • 模型   │  │  └──────────────────────────────────────┘ │  │  │  │
│  │  │  │ • 监控   │  │                                            │  │  │  │
│  │  │  │ • 日志   │  │  {children}                               │  │  │  │
│  │  │  │ • 账单   │  │                                            │  │  │  │
│  │  │  │ • 设置   │  │                                            │  │  │  │
│  │  │  └──────────┘  └────────────────────────────────────────────┘  │  │  │
│  │  └─────────────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  UI 组件:                                                                    │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  BcButton - 按钮 (primary/secondary/danger, sm/md/lg, loading)       │  │
│  │  BcInput - 输入 (text/password/email, placeholder, on_input)          │  │
│  │  BcCard - 卡片 (title, children)                                      │  │
│  │  BcTable - 表格 (columns, data)                                       │  │
│  │  BcModal - 模态框 (open, title, on_close)                             │  │
│  │  Toast - 消息提示 (success/info/warning/error, auto_dismiss)          │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  装饰组件:                                                                   │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  GlassCard - 玻璃态卡片 (半透明背景 + 模糊效果)                       │  │
│  │  AuroraBackground - 极光背景 (渐变动画背景)                           │  │
│  │  Logo - Logo 组件 (带/不带文字)                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第八层：认证上下文

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          认证上下文                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  AuthContext (Dioxus 状态)                                                   │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  pub struct AuthContext {                                              │  │
│  │      pub user: Option<User>,           // 当前用户                    │  │
│  │      pub token: Option<String>,        // JWT Token                   │  │
│  │      pub is_authenticated: bool,       // 是否已认证                  │  │
│  │  }                                                                     │  │
│  │                                                                        │  │
│  │  方法:                                                                  │  │
│  │  ├── login(username, password) → Result<()>                           │  │
│  │  ├── logout()                                                          │  │
│  │  ├── refresh()                                                         │  │
│  │  └── has_role(role) -> bool                                           │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  使用示例:                                                                   │
│  let auth = use_context::<AuthContext>();                                    │
│  if !auth.is_authenticated {                                                 │
│      return rsx! { Redirect { to: "/login" } };                             │
│  }                                                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第九层：运行模式

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              运行模式                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  GUI 模式 (Windows):                                                        │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  cargo run                                                             │  │
│  │  启动原生桌面应用:                                                      │  │
│  │  ├── Windows 11 Fluent Design                                         │  │
│  │  ├── 系统托盘图标                                                      │  │
│  │  └── 原生窗口管理                                                      │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  LiveView 模式 (Linux):                                                     │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  cargo run -- server                                                   │  │
│  │  启动 Web 服务:                                                         │  │
│  │  ├── 服务端渲染                                                        │  │
│  │  ├── 浏览器访问                                                        │  │
│  │  └── 实时状态同步                                                      │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第十层：文件级详解

### lib.rs - 模块入口

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                               lib.rs                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  模块声明:                                                                   │
│  pub mod app;                                                                │
│  pub mod components;                                                         │
│  pub mod pages;                                                              │
│                                                                              │
│  核心函数:                                                                   │
│  pub fn liveview_router(db: Arc<Database>) -> Router<DioxusRouterContext>   │
│  // 创建 LiveView 路由器                                                    │
│                                                                              │
│  路由配置:                                                                   │
│  Route::new()                                                                │
│      .route("/", home::Home)                                                 │
│      .route("/login", login::Login)                                          │
│      .route("/dashboard", dashboard::Dashboard)                              │
│      .route("/models", models::Models)                                       │
│      .route("/monitor", monitor::Monitor)                                    │
│      .route("/logs", logs::Logs)                                             │
│      .route("/settings", settings::Settings)                                 │
│      .route("/billing", billing::Billing)                                    │
│      .route("/playground", playground::Playground)                           │
│      .route("/api", api::Api)                                                │
│      .route("/connect", connect::Connect)                                    │
│      .route("/deploy", deploy::Deploy)                                       │
│      .route("/user", user::User)                                             │
│      .fallback(not_found::NotFound)                                          │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### main.rs - GUI 启动入口

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              main.rs                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  fn main() {                                                                 │
│      dioxus::launch(app::App);                                               │
│  }                                                                           │
│                                                                              │
│  启动模式:                                                                   │
│  ├── Desktop (Windows/macOS/Linux)                                          │
│  │   └── 使用 dioxus desktop renderer                                       │
│  └── LiveView (Web)                                                         │
│      └── 通过 server 启动                                                   │
│                                                                              │
│  特性:                                                                       │
│  ├── Windows 11 Fluent Design                                               │
│  ├── 系统托盘图标                                                           │
│  └── 原生窗口装饰                                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### app.rs - 根组件

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              app.rs                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  pub fn App(cx: Scope) -> Element {                                          │
│      // 提供全局上下文                                                       │
│      // AuthContext, ThemeContext, etc.                                      │
│                                                                              │
│      rsx! {                                                                  │
│          Router {                                                            │
│              // 路由配置                                                     │
│          }                                                                   │
│      }                                                                       │
│  }                                                                           │
│                                                                              │
│  上下文提供:                                                                 │
│  ├── AuthContext          // 认证状态                                       │
│  ├── ThemeContext         // 主题设置                                       │
│  └── LanguageContext      // 语言设置                                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### pages/mod.rs - 页面模块

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           pages/mod.rs                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  模块声明:                                                                   │
│  pub mod home;                                                               │
│  pub mod login;                                                              │
│  pub mod dashboard;                                                          │
│  pub mod models;                                                             │
│  pub mod monitor;                                                            │
│  pub mod logs;                                                               │
│  pub mod settings;                                                           │
│  pub mod billing;                                                            │
│  pub mod playground;                                                         │
│  pub mod api;                                                                │
│  pub mod connect;                                                            │
│  pub mod deploy;                                                             │
│  pub mod user;                                                               │
│  pub mod not_found;                                                          │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### pages/home.rs - 首页

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           pages/home.rs                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  pub fn Home(cx: Scope) -> Element {                                         │
│      // 显示欢迎信息                                                         │
│      // 系统状态概览                                                         │
│      // 快速入口链接                                                         │
│  }                                                                           │
│                                                                              │
│  组件结构:                                                                   │
│  rsx! {                                                                      │
│      Layout {                                                                │
│          div { class: "welcome", ... }                                       │
│          div { class: "quick-links", ... }                                  │
│          div { class: "stats-overview", ... }                               │
│      }                                                                       │
│  }                                                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### pages/login.rs - 登录页

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          pages/login.rs                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  pub fn Login(cx: Scope) -> Element {                                        │
│      // 用户名输入                                                           │
│      // 密码输入                                                             │
│      // 登录按钮                                                             │
│      // 错误提示                                                             │
│  }                                                                           │
│                                                                              │
│  状态管理:                                                                   │
│  let username = use_state(cx, || String::new());                            │
│  let password = use_state(cx, || String::new());                            │
│  let error = use_state(cx, || None);                                         │
│                                                                              │
│  事件处理:                                                                   │
│  onsubmit: move |_| {                                                        │
│      // 调用 auth_service::login()                                           │
│      // 成功 → 保存 Token → 跳转 Dashboard                                  │
│      // 失败 → 显示错误                                                      │
│  }                                                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### pages/dashboard.rs - 仪表盘

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        pages/dashboard.rs                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  pub fn Dashboard(cx: Scope) -> Element {                                    │
│      // 今日请求数                                                           │
│      // Token 使用量                                                         │
│      // 费用统计                                                             │
│      // 趋势图表                                                             │
│  }                                                                           │
│                                                                              │
│  数据获取:                                                                   │
│  use_future(cx, (), |_| {                                                    │
│      async move {                                                            │
│          let stats = log_service::get_stats().await;                        │
│          // 更新状态                                                        │
│      }                                                                       │
│  });                                                                         │
│                                                                              │
│  组件:                                                                       │
│  ├── StatsCard         // 统计卡片                                          │
│  ├── LineChart         // 折线图                                            │
│  └── PieChart          // 饼图                                              │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### pages/monitor.rs - 系统监控

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         pages/monitor.rs                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  pub fn Monitor(cx: Scope) -> Element {                                      │
│      // CPU 使用率                                                           │
│      // 内存使用                                                             │
│      // 磁盘使用                                                             │
│      // 实时刷新                                                             │
│  }                                                                           │
│                                                                              │
│  自动刷新:                                                                   │
│  use_future(cx, (), |_| {                                                    │
│      async move {                                                            │
│          loop {                                                              │
│              let info = monitor_service::get_system_info().await;           │
│              // 更新状态                                                    │
│              tokio::time::sleep(Duration::from_secs(5)).await;              │
│          }                                                                   │
│      }                                                                       │
│  });                                                                         │
│                                                                              │
│  组件:                                                                       │
│  ├── CpuGauge          // CPU 仪表                                          │
│  ├── MemoryBar         // 内存进度条                                        │
│  └── DiskList          // 磁盘列表                                          │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### pages/logs.rs - 日志查询

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          pages/logs.rs                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  pub fn Logs(cx: Scope) -> Element {                                         │
│      // 日志列表                                                             │
│      // 时间筛选                                                             │
│      // 状态码筛选                                                           │
│      // 分页                                                                 │
│  }                                                                           │
│                                                                              │
│  查询参数:                                                                   │
│  let start_time = use_state(cx, || None);                                   │
│  let end_time = use_state(cx, || None);                                     │
│  let status_code = use_state(cx, || None);                                  │
│  let page = use_state(cx, || 1);                                             │
│                                                                              │
│  组件:                                                                       │
│  ├── DateRangePicker   // 日期范围选择                                      │
│  ├── StatusFilter      // 状态码筛选                                        │
│  ├── LogTable          // 日志表格                                          │
│  └── Pagination        // 分页                                              │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### pages/billing.rs - 账单

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         pages/billing.rs                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  pub fn Billing(cx: Scope) -> Element {                                      │
│      // 余额显示 (USD/CNY)                                                   │
│      // 充值记录                                                             │
│      // 消费明细                                                             │
│      // 汇率显示                                                             │
│  }                                                                           │
│                                                                              │
│  显示数据:                                                                   │
│  ├── balance_usd       // USD 余额                                          │
│  ├── balance_cny       // CNY 余额                                          │
│  ├── exchange_rate     // 当前汇率                                          │
│  └── transactions      // 交易记录                                          │
│                                                                              │
│  组件:                                                                       │
│  ├── BalanceCard       // 余额卡片                                          │
│  ├── TransactionList   // 交易列表                                          │
│  └── ExchangeRateDisplay // 汇率显示                                        │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### pages/playground.rs - API 测试

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       pages/playground.rs                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  pub fn Playground(cx: Scope) -> Element {                                   │
│      // 请求编辑器                                                           │
│      // 响应查看                                                             │
│      // 流式输出                                                             │
│      // 历史记录                                                             │
│  }                                                                           │
│                                                                              │
│  功能:                                                                       │
│  ├── 选择模型                                                               │
│  ├── 编辑 JSON 请求体                                                       │
│  ├── 发送请求                                                               │
│  ├── 查看响应 (JSON/原始)                                                   │
│  ├── 流式输出实时显示                                                       │
│  └── 保存常用请求                                                           │
│                                                                              │
│  组件:                                                                       │
│  ├── ModelSelector     // 模型选择                                          │
│  ├── JsonEditor        // JSON 编辑器                                       │
│  ├── ResponseViewer    // 响应查看                                          │
│  └── StreamOutput      // 流式输出                                          │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### components/mod.rs - 组件模块

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        components/mod.rs                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  模块声明:                                                                   │
│  pub mod layout;                                                             │
│  pub mod guest_layout;                                                       │
│                                                                              │
│  重导出:                                                                     │
│  pub use layout::Layout;                                                     │
│  pub use guest_layout::GuestLayout;                                          │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### components/layout.rs - 主布局

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        components/layout.rs                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  pub fn Layout(cx: Scope, children: Element) -> Element {                    │
│      rsx! {                                                                  │
│          div { class: "app-container",                                       │
│              Sidebar { }                                                     │
│              div { class: "main-content",                                    │
│                  TitleBar { }                                                │
│                  div { class: "content", {children} }                       │
│              }                                                               │
│          }                                                                   │
│      }                                                                       │
│  }                                                                           │
│                                                                              │
│  布局结构:                                                                   │
│  ┌────────────┬──────────────────────────────────────┐                      │
│  │            │ TitleBar                            │                      │
│  │  Sidebar   ├──────────────────────────────────────┤                      │
│  │            │                                      │                      │
│  │  • 首页    │                                      │                      │
│  │  • 仪表盘  │           {children}                │                      │
│  │  • 模型    │                                      │                      │
│  │  • 监控    │                                      │                      │
│  │  • 日志    │                                      │                      │
│  │  • 账单    │                                      │                      │
│  │  • 设置    │                                      │                      │
│  │            │                                      │                      │
│  └────────────┴──────────────────────────────────────┘                      │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### components/guest_layout.rs - 访客布局

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    components/guest_layout.rs                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  pub fn GuestLayout(cx: Scope, children: Element) -> Element {               │
│      // 无侧边栏的布局                                                       │
│      // 用于登录/注册页面                                                   │
│  }                                                                           │
│                                                                              │
│  布局结构:                                                                   │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                                                                       │  │
│  │                          {children}                                   │  │
│  │                                                                       │  │
│  │                                                                       │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  背景: AuroraBackground (极光动画效果)                                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 依赖关系

```
burncloud-client
├── burncloud-database    # 数据库访问
├── burncloud-service-*   # 服务层
│   ├── service-monitor
│   ├── service-user
│   └── ...
├── 子 crates
│   ├── client-shared
│   ├── client-monitor
│   ├── client-api
│   ├── client-deploy
│   ├── client-register
│   └── client-models
└── external
    ├── dioxus           # UI 框架
    ├── dioxus-liveview  # LiveView 支持
    ├── dioxus-router    # 路由
    └── reqwest          # HTTP 客户端
```
