# client-shared

客户端共享库，提供组件、服务和工具函数。

## 🧅 第一层：在 Client 中的位置

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          burncloud-client                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌────────────────────────────────────────────────────────────────────┐    │
│   │                        客户端模块                                   │    │
│   │                                                                     │    │
│   │   ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐    │    │
│   │   │ pages           │  │ components      │  │★client-shared   │    │    │
│   │   │                 │  │                 │  │                 │    │    │
│   │   │ 页面视图        │  │ UI 组件         │  │ 共享代码        │    │    │
│   │   │ 路由配置        │  │ 布局组件        │  │ 服务层          │    │    │
│   │   │ 页面逻辑        │  │ 交互组件        │  │ 工具函数        │    │    │
│   │   └─────────────────┘  └─────────────────┘  └─────────────────┘    │    │
│   │                                                                     │    │
│   └────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

client-shared 的职责:
├── 共享 UI 组件
├── API 服务层
├── 认证上下文
├── 工具函数
└── 国际化支持
```

## 🧅 第二层：文件结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       client-shared 文件结构                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  crates/client/crates/client-shared/src/                                     │
│  │                                                                           │
│  ├── lib.rs              # 模块入口                                         │
│  ├── main.rs             # 应用入口 (GUI)                                   │
│  ├── styles.rs           # 全局样式                                         │
│  ├── i18n.rs             # 国际化                                           │
│  ├── api_client.rs       # HTTP 客户端                                      │
│  ├── auth_context.rs     # 认证上下文                                       │
│  │                                                                           │
│  ├── components/         # UI 组件                                          │
│  │   ├── mod.rs                                                              │
│  │   ├── layout.rs          # 布局                                          │
│  │   ├── sidebar.rs         # 侧边栏                                        │
│  │   ├── title_bar.rs       # 标题栏                                        │
│  │   ├── bc_button.rs       # 按钮                                          │
│  │   ├── bc_input.rs        # 输入框                                        │
│  │   ├── bc_card.rs         # 卡片                                          │
│  │   ├── bc_table.rs        # 表格                                          │
│  │   ├── bc_modal.rs        # 模态框                                        │
│  │   ├── bc_badge.rs        # 徽章                                          │
│  │   ├── toast.rs           # 通知                                          │
│  │   └── ...               # 其他组件                                       │
│  │                                                                           │
│  ├── services/           # API 服务                                         │
│  │   ├── mod.rs                                                              │
│  │   ├── auth_service.rs    # 认证                                          │
│  │   ├── user_service.rs    # 用户                                          │
│  │   ├── channel_service.rs # 通道                                          │
│  │   ├── token_service.rs   # Token                                         │
│  │   ├── monitor_service.rs # 监控                                          │
│  │   ├── log_service.rs     # 日志                                          │
│  │   └── usage_service.rs   # 使用量                                        │
│  │                                                                           │
│  └── utils/              # 工具函数                                         │
│      ├── mod.rs                                                              │
│      ├── storage.rs         # 本地存储                                      │
│      └── validation.rs      # 验证                                          │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第三层：认证上下文

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          认证上下文                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  auth_context.rs - 全局认证状态                                              │
│                                                                              │
│  pub struct AuthContext {                                                    │
│      pub is_authenticated: bool,     // 是否已认证                          │
│      pub user: Option<User>,         // 当前用户                            │
│      pub token: Option<String>,      // JWT Token                           │
│  }                                                                           │
│                                                                              │
│  功能:                                                                       │
│  ├── 登录状态管理                                                           │
│  ├── Token 存储 (localStorage)                                              │
│  ├── 自动登录 (Token 验证)                                                  │
│  └── 登出 (清除状态)                                                        │
│                                                                              │
│  使用方式:                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  // 在根组件提供 Context                                             │   │
│  │  let auth = use_state(|| AuthContext::new());                        │   │
│  │  rsx! {                                                              │   │
│  │      context_provider { auth.clone() }                               │   │
│  │      App {}                                                          │   │
│  │  }                                                                   │   │
│  │                                                                      │   │
│  │  // 在子组件中使用                                                   │   │
│  │  let auth = use_context::<AuthContext>();                            │   │
│  │  if !auth.is_authenticated {                                         │   │
│  │      return rsx! { Login {} };                                       │   │
│  │  }                                                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第四层：API 服务层

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          API 服务层                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  api_client.rs - HTTP 客户端封装                                             │
│  ───────────────────────────────                                             │
│  pub struct ApiClient {                                                      │
│      base_url: String,              // API 基础 URL                         │
│      client: reqwest::Client,       // HTTP 客户端                          │
│  }                                                                           │
│                                                                              │
│  功能:                                                                       │
│  ├── 统一的请求发送                                                         │
│  ├── 自动添加认证头                                                         │
│  ├── 错误处理                                                               │
│  └── 响应解析                                                               │
│                                                                              │
│  ─────────────────────────────────────────────────────────────────────────── │
│                                                                              │
│  服务模块:                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  auth_service.rs     # 认证服务                                      │   │
│  │  ├── login(username, password) → Token                              │   │
│  │  ├── register(username, password) → User                            │   │
│  │  └── validate_token(token) → bool                                   │   │
│  │                                                                      │   │
│  │  user_service.rs     # 用户服务                                      │   │
│  │  ├── get_users() → Vec<User>                                        │   │
│  │  ├── create_user(user) → User                                       │   │
│  │  └── update_user(id, user) → User                                   │   │
│  │                                                                      │   │
│  │  channel_service.rs  # 通道服务                                      │   │
│  │  ├── get_channels() → Vec<Channel>                                  │   │
│  │  └── update_channel(id, channel) → Channel                          │   │
│  │                                                                      │   │
│  │  token_service.rs    # Token 服务                                    │   │
│  │  ├── get_tokens() → Vec<Token>                                      │   │
│  │  ├── create_token(name) → Token                                     │   │
│  │  └── delete_token(id)                                               │   │
│  │                                                                      │   │
│  │  monitor_service.rs  # 监控服务                                      │   │
│  │  └── get_system_metrics() → SystemMetrics                           │   │
│  │                                                                      │   │
│  │  log_service.rs      # 日志服务                                      │   │
│  │  └── get_logs(filter) → Vec<Log>                                    │   │
│  │                                                                      │   │
│  │  usage_service.rs    # 使用量服务                                    │   │
│  │  └── get_usage(user_id) → Usage                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第五层：工具函数

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          工具函数                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  storage.rs - 本地存储                                                       │
│  ─────────────────────                                                       │
│  功能:                                                                       │
│  ├── save_token(token) → Result<()>     // 保存 Token 到 localStorage       │
│  ├── load_token() → Option<String>      // 从 localStorage 读取 Token       │
│  ├── clear_token()                      // 清除 Token                       │
│  └── save_preference(key, value)        // 保存用户偏好                     │
│                                                                              │
│  validation.rs - 验证函数                                                    │
│  ──────────────────────                                                      │
│  功能:                                                                       │
│  ├── validate_email(email) -> bool      // 邮箱格式验证                     │
│  ├── validate_password(pwd) -> Result   // 密码强度验证                     │
│  └── validate_username(name) -> bool    // 用户名验证                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第六层：国际化

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          国际化                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  i18n.rs - 多语言支持                                                        │
│                                                                              │
│  支持的语言:                                                                 │
│  ├── 中文 (zh-CN)                                                           │
│  └── English (en-US)                                                        │
│                                                                              │
│  使用方式:                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  let i18n = use_i18n();                                              │   │
│  │  rsx! {                                                              │   │
│  │      h1 { "{i18n.welcome}" }                                         │   │
│  │      button { "{i18n.login}" }                                       │   │
│  │  }                                                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  语言切换:                                                                   │
│  ├── 自动检测系统语言                                                       │
│  ├── 用户手动切换                                                           │
│  └── 持久化到 localStorage                                                  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 依赖关系

```
client-shared
├── dioxus             # UI 框架
├── dioxus-router      # 路由
├── reqwest            # HTTP 客户端
├── serde              # 序列化
├── gloo-storage       # Web 存储
└── wasm-bindgen       # WebAssembly 绑定
```
