# api/

REST API 模块，提供所有管理接口。

## 🧅 第一层：在 Server 中的位置

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          burncloud-server                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   HTTP 请求                                                                  │
│       │                                                                      │
│       ▼                                                                      │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                         axum Router                                  │   │
│   │                                                                      │   │
│   │   路由分发:                                                          │   │
│   │   /api/*      ───────────►  ★ api/ (当前)                           │   │
│   │   /v1/*       ───────────►  Router fallback                         │   │
│   │   /*          ───────────►  LiveView 或静态文件                      │   │
│   │                                                                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│       │                                                                      │
│       ▼                                                                      │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                         api/ 模块                                    │   │
│   │                                                                      │   │
│   │   ├── auth.rs      登录/登出/验证                                    │   │
│   │   ├── user.rs      用户增删改查                                      │   │
│   │   ├── channel.rs   通道管理                                          │   │
│   │   ├── token.rs     API Token 管理                                    │   │
│   │   ├── group.rs     分组管理                                          │   │
│   │   ├── log.rs       日志查询                                          │   │
│   │   └── monitor.rs   系统监控                                          │   │
│   │                                                                      │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

api/ 的职责:
├── 提供 RESTful API
├── 用户认证和授权
├── 资源管理 CRUD
├── 数据查询和统计
└── 系统监控接口
```

## 🧅 第二层：文件结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            api/ 文件结构                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  crates/server/src/api/                                                      │
│  │                                                                           │
│  ├── mod.rs           # 模块定义 + 路由注册                                 │
│  ├── auth.rs          # 认证相关 API                                        │
│  ├── user.rs          # 用户管理 API                                        │
│  ├── channel.rs       # 通道管理 API                                        │
│  ├── token.rs         # Token 管理 API                                      │
│  ├── group.rs         # 分组管理 API                                        │
│  ├── log.rs           # 日志查询 API                                        │
│  └── monitor.rs       # 系统监控 API                                        │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第三层：mod.rs 路由注册

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              mod.rs                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  模块声明:                                                                   │
│  pub mod auth;                                                               │
│  pub mod user;                                                               │
│  pub mod channel;                                                            │
│  pub mod token;                                                              │
│  pub mod group;                                                              │
│  pub mod log;                                                                │
│  pub mod monitor;                                                            │
│                                                                              │
│  路由定义:                                                                   │
│  pub fn routes(state: AppState) -> Router {                                  │
│      Router::new()                                                           │
│          // 认证路由 (无需 Token)                                            │
│          .route("/api/auth/login", post(auth::login))                        │
│          .route("/api/auth/logout", post(auth::logout))                      │
│          .route("/api/auth/me", get(auth::me))                               │
│                                                                              │
│          // 需要认证的路由                                                   │
│          .nest("/api", authenticated_routes(state))                          │
│  }                                                                           │
│                                                                              │
│  fn authenticated_routes(state: AppState) -> Router {                        │
│      Router::new()                                                           │
│          // 用户管理                                                         │
│          .route("/users", get(user::list).post(user::create))               │
│          .route("/users/:id", get(user::get).put(user::update)              │
│              .delete(user::delete))                                          │
│                                                                              │
│          // 通道管理                                                         │
│          .route("/channels", get(channel::list).post(channel::create))      │
│          .route("/channels/:id", ...)                                        │
│                                                                              │
│          // Token 管理                                                       │
│          .route("/tokens", get(token::list).post(token::create))            │
│          .route("/tokens/:id", delete(token::delete))                        │
│                                                                              │
│          // 分组管理                                                         │
│          .route("/groups", get(group::list).post(group::create))            │
│                                                                              │
│          // 日志查询                                                         │
│          .route("/logs", get(log::list))                                     │
│          .route("/logs/stats", get(log::stats))                              │
│                                                                              │
│          // 系统监控                                                         │
│          .route("/monitor", get(monitor::get_system_info))                  │
│          .route("/monitor/cpu", get(monitor::get_cpu_usage))                │
│          .route("/monitor/memory", get(monitor::get_memory_usage))          │
│          .route("/monitor/disk", get(monitor::get_disk_usage))              │
│                                                                              │
│          // 认证中间件                                                       │
│          .layer(middleware::from_fn_with_state(state, auth_middleware))      │
│  }                                                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第四层：各文件详解

### auth.rs

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                             auth.rs                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  职责: 处理用户登录、登出和身份验证                                          │
│                                                                              │
│  API 端点:                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  POST /api/auth/login                                               │   │
│  │  请求: { "username": "admin", "password": "123456" }                │   │
│  │  响应: { "token": "eyJhbGc...", "user": {...} }                      │   │
│  │                                                                      │   │
│  │  POST /api/auth/logout                                              │   │
│  │  响应: 200 OK                                                        │   │
│  │                                                                      │   │
│  │  GET /api/auth/me                                                   │   │
│  │  响应: { "id": "1", "username": "admin", "role": 100, ... }         │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  核心函数:                                                                   │
│  pub async fn login(State, Json<LoginRequest>) -> Result<Json<LoginResponse>>│
│  pub async fn logout() -> StatusCode                                        │
│  pub async fn me(claims: Claims) -> Json<User>                              │
│  pub async fn auth_middleware(req, next) -> Result<Response>                │
│                                                                              │
│  流程图:                                                                     │
│                                                                              │
│   请求 → 提取 Authorization 头                                               │
│               │                                                              │
│               ▼                                                              │
│         验证 JWT Token                                                       │
│               │                                                              │
│         ┌─────┴─────┐                                                        │
│         │           │                                                        │
│         ▼           ▼                                                        │
│       有效         无效                                                      │
│         │           │                                                        │
│         ▼           ▼                                                        │
│    提取 Claims   返回 401                                                   │
│         │                                                                    │
│         ▼                                                                    │
│    继续处理请求                                                              │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### user.rs

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                             user.rs                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  职责: 用户 CRUD 操作                                                        │
│                                                                              │
│  API 端点:                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  GET /api/users?page=1&per_page=20                                  │   │
│  │  响应: [{ "id": "1", "username": "user1", ... }, ...]               │   │
│  │                                                                      │   │
│  │  POST /api/users                                                    │   │
│  │  请求: { "username": "newuser", "password": "xxx", "role": 1 }      │   │
│  │  响应: { "id": "uuid", ... }                                        │   │
│  │                                                                      │   │
│  │  PUT /api/users/:id                                                 │   │
│  │  请求: { "balance_usd": 1000000000 }                                │   │
│  │                                                                      │   │
│  │  DELETE /api/users/:id                                              │   │
│  │  响应: 200 OK                                                        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  权限控制:                                                                   │
│  ├── list:   需要管理员 (role >= 10)                                        │
│  ├── create: 需要管理员 (role >= 10)                                        │
│  ├── update: 本人或管理员                                                   │
│  └── delete: 需要超级管理员 (role >= 100)                                   │
│                                                                              │
│  特殊处理:                                                                   │
│  ├── 密码存储为 bcrypt 哈希                                                 │
│  ├── 用户名必须唯一                                                          │
│  └── 余额为纳美元 (i64)                                                     │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### channel.rs

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           channel.rs                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  职责: 上游通道管理                                                          │
│                                                                              │
│  通道是什么?                                                                 │
│  ─────────────                                                               │
│  通道是连接到 LLM 提供商的配置，包含:                                        │
│  • 提供商类型 (OpenAI/Anthropic/Google/...)                                 │
│  • API 密钥                                                                  │
│  • 上游地址                                                                  │
│  • 支持的模型列表                                                            │
│                                                                              │
│  API 端点:                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  GET /api/channels                                                  │   │
│  │  响应: [{ "id": 1, "name": "OpenAI", "type": 1, ... }, ...]         │   │
│  │                                                                      │   │
│  │  POST /api/channels                                                 │   │
│  │  请求: {                                                             │   │
│  │    "name": "OpenAI Official",                                       │   │
│  │    "type": 1,                                                        │   │
│  │    "key": "sk-xxx",                                                 │   │
│  │    "base_url": "https://api.openai.com",                            │   │
│  │    "models": "gpt-4,gpt-3.5-turbo",                                 │   │
│  │    "group": "default",                                               │   │
│  │    "priority": 100                                                   │   │
│  │  }                                                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  Channel 结构:                                                               │
│  {                                                                           │
│    "id": 1,                         // 通道 ID                               │
│    "name": "OpenAI Official",       // 显示名称                             │
│    "type": 1,                       // 1=OpenAI, 14=Anthropic, ...         │
│    "key": "sk-***",                 // API 密钥 (脱敏显示)                  │
│    "base_url": "https://...",       // 上游地址                             │
│    "models": "gpt-4,gpt-3.5",       // 支持的模型                           │
│    "group": "default",              // 用户组                               │
│    "priority": 100,                 // 优先级 (越高越优先)                  │
│    "api_version": "2024-01-01"      // API 版本                            │
│  }                                                                           │
│                                                                              │
│  副作用:                                                                     │
│  创建/更新/删除后自动触发路由器配置重载                                      │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### token.rs

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                             token.rs                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  职责: API Token (sk-xxx) 管理                                               │
│                                                                              │
│  Token 是什么?                                                               │
│  ────────────────                                                            │
│  用户用来调用 LLM API 的密钥，格式: sk-xxxxxx                                │
│  每个 Token 关联一个用户，有配额限制                                          │
│                                                                              │
│  API 端点:                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  GET /api/tokens?user_id=xxx                                        │   │
│  │  响应: [{ "key": "sk-***", "name": "My Key", ... }, ...]            │   │
│  │                                                                      │   │
│  │  POST /api/tokens                                                   │   │
│  │  请求: { "user_id": "uuid", "name": "New Key", "quota_limit": 1000 }│   │
│  │  响应: { "key": "sk-abc123...", ... }  ← 仅创建时返回完整 key        │   │
│  │                                                                      │   │
│  │  DELETE /api/tokens/:id                                             │   │
│  │  响应: 200 OK                                                        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  Token 结构:                                                                 │
│  {                                                                           │
│    "id": "uuid",                                                             │
│    "user_id": "user_uuid",          // 所属用户                             │
│    "key": "sk-***",                 // 脱敏显示                             │
│    "name": "Production Key",        // Token 名称                           │
│    "status": 1,                     // 1=有效, 0=禁用                       │
│    "quota_limit": 1000000,          // 配额限制 (纳美元)                    │
│    "used_quota": 50000,             // 已使用                               │
│    "expired_time": null,            // 过期时间                             │
│    "accessed_time": "2024-01-01"    // 最后访问                             │
│  }                                                                           │
│                                                                              │
│  Token 生成:                                                                 │
│  格式: sk-{32位随机字符}                                                    │
│  例: sk-abc123def456ghi789jkl012mno345pqr                                   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### log.rs

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              log.rs                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  职责: 请求日志查询和统计                                                    │
│                                                                              │
│  API 端点:                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  GET /api/logs?start_time=2024-01-01&end_time=2024-01-31           │   │
│  │                                                                      │   │
│  │  查询参数:                                                           │   │
│  │  ├── start_time   开始时间                                           │   │
│  │  ├── end_time     结束时间                                           │   │
│  │  ├── user_id      按用户筛选                                         │   │
│  │  ├── status_code  按状态码筛选 (200, 400, 500, ...)                  │   │
│  │  ├── page         页码                                               │   │
│  │  └── per_page     每页数量                                           │   │
│  │                                                                      │   │
│  │  GET /api/logs/stats                                                │   │
│  │  响应: {                                                             │   │
│  │    "total_requests": 10000,                                          │   │
│  │    "total_tokens": 500000,                                           │   │
│  │    "total_cost": 15.50,                                              │   │
│  │    "avg_latency_ms": 1234.5,                                         │   │
│  │    "error_rate": 0.02                                                │   │
│  │  }                                                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  日志记录结构:                                                               │
│  {                                                                           │
│    "request_id": "uuid",            // 请求唯一标识                          │
│    "user_id": "user_uuid",          // 用户 ID                              │
│    "path": "/v1/chat/completions",  // 请求路径                             │
│    "upstream_id": "channel_1",      // 上游通道                             │
│    "status_code": 200,              // HTTP 状态码                          │
│    "latency_ms": 1234,              // 延迟毫秒                             │
│    "prompt_tokens": 100,            // 输入 Token                           │
│    "completion_tokens": 200,        // 输出 Token                           │
│    "cost": 0.005,                   // 成本美元                             │
│    "created_at": "2024-01-01T12:00:00Z"                                     │
│  }                                                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### monitor.rs

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            monitor.rs                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  职责: 系统监控数据 API                                                      │
│                                                                              │
│  API 端点:                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  GET /api/monitor              # 获取所有监控数据                     │   │
│  │  GET /api/monitor/cpu          # CPU 使用率                           │   │
│  │  GET /api/monitor/memory       # 内存使用                             │   │
│  │  GET /api/monitor/disk         # 磁盘使用                             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  响应示例:                                                                   │
│  {                                                                           │
│    "cpu_usage": 45.5,               // CPU 使用率 %                         │
│    "memory": {                                                               │
│      "total": 16777216,             // 总内存 KB                            │
│      "used": 8388608,               // 已使用 KB                            │
│      "usage_percent": 50.0          // 使用率 %                             │
│    },                                                                        │
│    "disks": [                                                                │
│      {                                                                       │
│        "mount_point": "/",                                                  │
│        "total": 500000000,         // 总空间 KB                              │
│        "used": 250000000,          // 已使用 KB                              │
│        "usage_percent": 50.0       // 使用率 %                              │
│      }                                                                       │
│    ],                                                                        │
│    "uptime_secs": 86400             // 运行时间秒                            │
│  }                                                                           │
│                                                                              │
│  数据来源:                                                                   │
│  SystemMonitorService (来自 service-monitor)                                 │
│  自动每 5 秒采集一次                                                         │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 依赖关系

```
api/
├── burncloud-database
│   ├── database-user    # 用户数据
│   ├── database-router  # 通道/Token/日志
│   └── database-models  # 价格数据
├── burncloud-service-monitor  # 监控数据
├── burncloud-common
│   └── types, error, utils
└── external
    ├── axum             # Web 框架
    ├── serde_json       # JSON 处理
    └── tower-http       # 中间件
```
