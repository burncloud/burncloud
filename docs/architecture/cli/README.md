# burncloud-cli

命令行工具，提供管理操作接口。

## 🧅 第一层：系统中的位置

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          BurnCloud 系统架构                                  │
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                          burncloud (main)                              │  │
│  │                                                                        │  │
│  │   cargo run            → GUI 模式                                     │  │
│  │   cargo run -- server  → 服务端模式                                   │  │
│  │   cargo run -- router  → 纯路由模式                                   │  │
│  │   cargo run -- client  → GUI 客户端                                   │  │
│  │   cargo run -- code    → 编程模式                                     │  │
│  │   burncloud <cmd>      → ★ CLI 工具 (当前)                            │  │
│  │                                                                        │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

CLI 的职责:
├── 价格管理 (price/tiered)
├── 汇率管理 (currency)
├── 通道管理 (channel)
├── Token 管理 (token)
├── 协议配置 (protocol)
└── 批量操作
```

## 🧅 第二层：CLI 整体架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            burncloud-cli                                     │
│                         (Command Line Tool)                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                          commands.rs                                   │  │
│  │                          (命令路由)                                    │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                      │                                       │
│            ┌─────────────────────────┼─────────────────────────┐            │
│            │                         │                         │            │
│            ▼                         ▼                         ▼            │
│  ┌──────────────────┐    ┌──────────────────┐    ┌──────────────────────┐  │
│  │    price.rs      │    │   currency.rs    │    │    channel.rs        │  │
│  │                  │    │                  │    │                      │  │
│  │ price list       │    │ currency list    │    │ channel list         │  │
│  │ price set        │    │ currency get     │    │ channel add          │  │
│  │ price get        │    │ currency set     │    │ channel update       │  │
│  │ price delete     │    │                  │    │ channel delete       │  │
│  └──────────────────┘    └──────────────────┘    └──────────────────────┘  │
│                                                                              │
│  ┌──────────────────┐    ┌──────────────────┐    ┌──────────────────────┐  │
│  │    token.rs      │    │   protocol.rs    │    │    client.rs         │  │
│  │                  │    │                  │    │                      │  │
│  │ token list       │    │ protocol list    │    │ API 客户端           │  │
│  │ token create     │    │ protocol get     │    │                      │  │
│  │ token delete     │    │ protocol set     │    │                      │  │
│  └──────────────────┘    └──────────────────┘    └──────────────────────┘  │
│                                                                              │
│  ┌──────────────────┐                                                       │
│  │    output.rs     │                                                       │
│  │                  │                                                       │
│  │ 输出格式化       │                                                       │
│  └──────────────────┘                                                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第三层：源码结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            cli/ 结构                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  crates/cli/                                                                 │
│  └── src/                                                                    │
│      ├── lib.rs              # 入口                                         │
│      ├── commands.rs         # 命令路由和分发                               │
│      ├── price.rs            # 价格管理命令                                 │
│      ├── currency.rs         # 汇率管理命令                                 │
│      ├── channel.rs          # 通道管理命令                                 │
│      ├── token.rs            # Token 管理命令                               │
│      ├── protocol.rs         # 协议配置命令                                 │
│      ├── client.rs           # API 客户端                                   │
│      └── output.rs           # 输出格式化                                   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第四层：模块清单

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              模块清单                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  模块          │ 文件             │ 职责                                    │
│  ─────────────┼──────────────────┼──────────────────────────────────────── │
│  lib.rs       │ lib.rs           │ 入口                                    │
│  commands     │ commands.rs      │ 命令路由和分发                          │
│  price        │ price.rs         │ 价格管理命令                            │
│  currency     │ currency.rs      │ 汇率管理命令                            │
│  channel      │ channel.rs       │ 通道管理命令                            │
│  token        │ token.rs         │ Token 管理命令                          │
│  protocol     │ protocol.rs      │ 协议配置命令                            │
│  client       │ client.rs        │ API 客户端                              │
│  output       │ output.rs        │ 输出格式化                              │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第五层：CLI 命令详解

### 价格管理

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              价格管理                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  # 列出所有模型价格                                                          │
│  burncloud price list                                                        │
│                                                                              │
│  # 设置模型价格                                                              │
│  burncloud price set <model> --input <price> --output <price>               │
│                                                                              │
│  # 获取模型价格详情                                                          │
│  burncloud price get <model>                                                 │
│                                                                              │
│  # 删除模型价格                                                              │
│  burncloud price delete <model>                                              │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 分段定价管理

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            分段定价管理                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  # 列出分段定价                                                              │
│  burncloud tiered list-tiers <model> [--region <cn|international>]          │
│                                                                              │
│  # 添加分段定价                                                              │
│  burncloud tiered add-tier <model> \                                        │
│      --tier-start <tokens> --tier-end <tokens> \                            │
│      --input-price <price> --output-price <price> [--region <region>]       │
│                                                                              │
│  # 导入分段定价                                                              │
│  burncloud tiered import-tiered <file.json>                                  │
│                                                                              │
│  # 检查是否有分段定价                                                        │
│  burncloud tiered check-tiered <model>                                       │
│                                                                              │
│  # 删除分段定价                                                              │
│  burncloud tiered delete-tiers <model> [--region <region>]                  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 汇率管理

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              汇率管理                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  # 列出汇率                                                                  │
│  burncloud currency list                                                     │
│                                                                              │
│  # 获取汇率                                                                  │
│  burncloud currency get <from> <to>                                          │
│                                                                              │
│  # 设置汇率                                                                  │
│  burncloud currency set <from> <to> <rate>                                   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 通道/Token/协议管理

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          通道/Token/协议管理                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  # 通道管理                                                                  │
│  burncloud channel list                                                      │
│  burncloud channel add                                                       │
│  burncloud channel update                                                    │
│  burncloud channel delete                                                    │
│                                                                              │
│  # Token 管理                                                                │
│  burncloud token list                                                        │
│  burncloud token create                                                      │
│  burncloud token delete                                                      │
│                                                                              │
│  # 协议配置                                                                  │
│  burncloud protocol list                                                     │
│  burncloud protocol get <channel_type>                                       │
│  burncloud protocol set <channel_type> <config.json>                         │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第六层：文件级详解

### lib.rs - 模块入口

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                               lib.rs                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  模块声明:                                                                   │
│  pub mod commands;                                                           │
│  pub mod price;                                                              │
│  pub mod currency;                                                           │
│  pub mod channel;                                                            │
│  pub mod token;                                                              │
│  pub mod protocol;                                                           │
│  pub mod client;                                                             │
│  pub mod output;                                                             │
│                                                                              │
│  入口函数:                                                                   │
│  pub async fn run() -> Result<()>                                            │
│  // 解析命令行参数并执行对应命令                                            │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### commands.rs - 命令路由

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            commands.rs                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  使用 clap 定义 CLI 结构:                                                    │
│                                                                              │
│  #[derive(Parser)]                                                           │
│  #[command(name = "burncloud")]                                              │
│  struct Cli {                                                                │
│      #[command(subcommand)]                                                  │
│      command: Commands,                                                      │
│  }                                                                           │
│                                                                              │
│  #[derive(Subcommand)]                                                       │
│  enum Commands {                                                             │
│      Price(PriceCommands),                                                   │
│      Tiered(TieredCommands),                                                 │
│      Currency(CurrencyCommands),                                             │
│      Channel(ChannelCommands),                                               │
│      Token(TokenCommands),                                                   │
│      Protocol(ProtocolCommands),                                             │
│  }                                                                           │
│                                                                              │
│  pub async fn dispatch(cli: Cli, db: &Database) -> Result<()>               │
│  // 根据命令分发到对应处理函数                                              │
│                                                                              │
│  命令层级:                                                                   │
│  burncloud price list                                                        │
│  burncloud tiered add-tier ...                                               │
│  burncloud currency set USD CNY 7.2                                          │
│  burncloud channel list                                                      │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### price.rs - 价格管理

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                             price.rs                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  #[derive(Subcommand)]                                                       │
│  pub enum PriceCommands {                                                    │
│      List,                                                                   │
│      Set { model: String, input: f64, output: f64, ... },                   │
│      Get { model: String },                                                  │
│      Delete { model: String },                                               │
│  }                                                                           │
│                                                                              │
│  pub async fn handle_price(cmd: PriceCommands, db: &Database) -> Result<()> │
│                                                                              │
│  实现函数:                                                                   │
│  async fn list_prices(db: &Database) -> Result<()>                           │
│  // 列出所有价格                                                            │
│                                                                              │
│  async fn set_price(db: &Database, model: &str, input: f64, output: f64)    │
│  // 设置价格 (自动转换为纳美元)                                             │
│                                                                              │
│  async fn get_price(db: &Database, model: &str) -> Result<()>               │
│  // 获取价格详情                                                            │
│                                                                              │
│  async fn delete_price(db: &Database, model: &str) -> Result<()>            │
│  // 删除价格                                                                │
│                                                                              │
│  价格存储格式: 纳美元/百万 Token                                             │
│  例: $0.002/百万 → 2_000_000 纳美元                                         │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### currency.rs - 汇率管理

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           currency.rs                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  #[derive(Subcommand)]                                                       │
│  pub enum CurrencyCommands {                                                 │
│      List,                                                                   │
│      Get { from: String, to: String },                                       │
│      Set { from: String, to: String, rate: f64 },                           │
│  }                                                                           │
│                                                                              │
│  pub async fn handle_currency(cmd: CurrencyCommands, db: &Database)         │
│      -> Result<()>                                                           │
│                                                                              │
│  实现函数:                                                                   │
│  async fn list_rates(db: &Database) -> Result<()>                            │
│  // 列出所有汇率                                                            │
│                                                                              │
│  async fn get_rate(db: &Database, from: &str, to: &str) -> Result<()>       │
│  // 获取汇率                                                                │
│                                                                              │
│  async fn set_rate(db: &Database, from: &str, to: &str, rate: f64)          │
│      -> Result<()>                                                           │
│  // 设置汇率 (存储为缩放 i64)                                               │
│                                                                              │
│  汇率存储: i64 缩放 (×10^9)                                                  │
│  例: USD/CNY 7.2 → 7_200_000_000                                            │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### channel.rs - 通道管理

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           channel.rs                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  #[derive(Subcommand)]                                                       │
│  pub enum ChannelCommands {                                                  │
│      List,                                                                   │
│      Add { ... },                                                            │
│      Update { id: i32, ... },                                                │
│      Delete { id: i32 },                                                     │
│  }                                                                           │
│                                                                              │
│  pub async fn handle_channel(cmd: ChannelCommands, db: &Database)           │
│      -> Result<()>                                                           │
│                                                                              │
│  Add 参数:                                                                   │
│  --name          通道名称                                                    │
│  --type          通道类型 (1=OpenAI, 14=Anthropic, ...)                     │
│  --key           API 密钥                                                    │
│  --base-url      上游地址                                                    │
│  --models        支持的模型 (逗号分隔)                                       │
│  --group         用户组                                                      │
│  --priority      优先级                                                      │
│                                                                              │
│  输出格式: 表格形式显示通道列表                                              │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### token.rs - Token 管理

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            token.rs                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  #[derive(Subcommand)]                                                       │
│  pub enum TokenCommands {                                                    │
│      List { user_id: Option<String> },                                       │
│      Create { user_id: String, name: String, quota: Option<i64> },          │
│      Delete { id: String },                                                  │
│  }                                                                           │
│                                                                              │
│  pub async fn handle_token(cmd: TokenCommands, db: &Database)               │
│      -> Result<()>                                                           │
│                                                                              │
│  Create 输出: 显示生成的 sk-xxx 格式 Token                                   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### protocol.rs - 协议配置

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           protocol.rs                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  #[derive(Subcommand)]                                                       │
│  pub enum ProtocolCommands {                                                 │
│      List,                                                                   │
│      Get { channel_type: i32 },                                              │
│      Set { channel_type: i32, config: PathBuf },                            │
│  }                                                                           │
│                                                                              │
│  pub async fn handle_protocol(cmd: ProtocolCommands, db: &Database)         │
│      -> Result<()>                                                           │
│                                                                              │
│  配置文件格式 (JSON):                                                        │
│  {                                                                           │
│    "channel_type": 1,                                                        │
│    "api_version": "2024-01-01",                                              │
│    "chat_endpoint": "/v1/chat/completions",                                  │
│    "request_mapping": [...],                                                 │
│    "response_mapping": [...]                                                 │
│  }                                                                           │
│                                                                              │
│  用途: 配置动态协议适配器                                                    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### client.rs - API 客户端

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            client.rs                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  pub struct ApiClient {                                                      │
│      base_url: String,                                                       │
│      client: reqwest::Client,                                                │
│  }                                                                           │
│                                                                              │
│  impl ApiClient {                                                            │
│      pub fn new(base_url: &str) -> Self                                      │
│                                                                              │
│      pub async fn get<T: DeserializeOwned>(&self, path: &str)               │
│          -> Result<T>                                                        │
│                                                                              │
│      pub async fn post<T: Serialize, R: DeserializeOwned>(                  │
│          &self, path: &str, body: &T) -> Result<R>                          │
│                                                                              │
│      pub async fn delete(&self, path: &str) -> Result<()>                   │
│  }                                                                           │
│                                                                              │
│  用途: 与 server REST API 通信的客户端封装                                   │
│  可选: 支持 HTTP 认证                                                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### output.rs - 输出格式化

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            output.rs                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  pub enum OutputFormat {                                                     │
│      Table,                              // 表格 (默认)                     │
│      Json,                              // JSON 格式                        │
│      Yaml,                              // YAML 格式                        │
│  }                                                                           │
│                                                                              │
│  pub fn format_table<T: TableFormat>(data: &[T]) -> String                  │
│  // 格式化为表格                                                            │
│                                                                              │
│  pub fn format_json<T: Serialize>(data: &T) -> String                       │
│  // 格式化为 JSON                                                           │
│                                                                              │
│  pub trait TableFormat {                                                     │
│      fn headers() -> Vec<&'static str>;                                      │
│      fn row(&self) -> Vec<String>;                                           │
│  }                                                                           │
│                                                                              │
│  实现: User, Token, Channel, Price, ... 的 TableFormat                      │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 依赖关系

```
burncloud-cli
├── burncloud-database      # 数据库访问
│   ├── database-models     # 价格模型
│   └── database-router     # 路由数据
├── burncloud-common        # 共享类型
└── external
    ├── clap                # 命令行参数解析
    └── tokio               # 异步运行时
```
