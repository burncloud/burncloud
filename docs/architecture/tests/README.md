# burncloud-tests

集成测试套件，包含 API 测试和 UI 测试。

## 🧅 第一层：系统中的位置

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          BurnCloud 系统架构                                  │
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                        🛠️ 辅助工具层                                   │  │
│  │                                                                        │  │
│  │   CLI ─── Download ─── AutoUpdate ─── Code ─── Core ─── ★ Tests       │  │
│  │                                                               当前     │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

Tests 的职责:
├── API 集成测试
├── UI 组件测试
├── 价格精度测试
├── 测试工具函数
└── Mock 数据生成
```

## 🧅 第二层：Tests 整体架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           burncloud-tests                                    │
│                         (Integration Tests)                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                          tests/                                        │  │
│  │                                                                        │  │
│  │   ┌─────────────────────────────────────────────────────────────────┐  │  │
│  │   │                    API 测试 (api/)                              │  │  │
│  │   │                                                                 │  │  │
│  │   │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │  │  │
│  │   │  │  auth.rs    │  │  user.rs    │  │ channel.rs  │             │  │  │
│  │   │  │ • 登录认证  │  │ • 用户 CRUD │  │ • 通道管理  │             │  │  │
│  │   │  │ • JWT 验证  │  │ • 权限检查  │  │ • 模型配置  │             │  │  │
│  │   │  └─────────────┘  └─────────────┘  └─────────────┘             │  │  │
│  │   │                                                                 │  │  │
│  │   │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │  │  │
│  │   │  │  relay.rs   │  │  log.rs     │  │ monitor.rs  │             │  │  │
│  │   │  │ • 请求转发  │  │ • 日志查询  │  │ • 系统监控  │             │  │  │
│  │   │  │ • 流式响应  │  │ • 统计分析  │  │ • 性能指标  │             │  │  │
│  │   │  └─────────────┘  └─────────────┘  └─────────────┘             │  │  │
│  │   └─────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                        │  │
│  │   ┌─────────────────────────────────────────────────────────────────┐  │  │
│  │   │                    UI 测试 (ui/)                                │  │  │
│  │   │                                                                 │  │  │
│  │   │  ┌─────────────┐  ┌─────────────┐                              │  │  │
│  │   │  │ basic_      │  │  runner.rs  │                              │  │  │
│  │   │  │ render.rs   │  │ • 测试运行器│                              │  │  │
│  │   │  │ • 页面渲染  │  │ • 组件验证  │                              │  │  │
│  │   │  └─────────────┘  └─────────────┘                              │  │  │
│  │   └─────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                        │  │
│  │   ┌─────────────────────────────────────────────────────────────────┐  │  │
│  │   │                    价格测试                                      │  │  │
│  │   │  pricing_u64_test.rs                                             │  │  │
│  │   │  • 纳美元精度测试 • 价格转换测试 • 边界条件测试                 │  │  │
│  │   └─────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                        │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                          common/                                       │  │
│  │  • 测试工具函数 • 测试环境设置 • Mock 数据生成                        │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第三层：源码结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              文件结构                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  crates/tests/                                                               │
│  ├── src/                                                                    │
│  │   └── lib.rs                     # 测试库入口                            │
│  │                                                                           │
│  └── tests/                                                                  │
│      ├── api_tests.rs               # API 测试入口                          │
│      ├── ui_tests.rs                # UI 测试入口                           │
│      │                                                                       │
│      ├── api/                        # API 测试                             │
│      │   ├── mod.rs                  # 模块定义                             │
│      │   ├── auth.rs                 # 认证测试                             │
│      │   ├── auth_handlers.rs        # 认证处理器测试                       │
│      │   ├── user.rs                 # 用户测试                             │
│      │   ├── channel.rs              # 通道测试                             │
│      │   ├── relay.rs                # 转发测试                             │
│      │   ├── log.rs                  # 日志测试                             │
│      │   ├── monitor.rs              # 监控测试                             │
│      │   ├── status.rs               # 状态测试                             │
│      │   ├── ability_routing.rs      # 路由测试                             │
│      │   └── claude_relay.rs         # Claude 中继测试                      │
│      │                                                                       │
│      ├── ui/                         # UI 测试                              │
│      │   ├── mod.rs                  # 模块定义                             │
│      │   ├── runner.rs               # 测试运行器                           │
│      │   └── basic_render.rs         # 基础渲染测试                         │
│      │                                                                       │
│      ├── common/                     # 公共测试工具                         │
│      │   └── mod.rs                  # 工具函数                             │
│      │                                                                       │
│      └── pricing_u64_test.rs         # 价格精度测试                         │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第四层：测试分类

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              测试分类                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                        单元测试 (Unit Tests)                          │  │
│  │                                                                        │  │
│  │   位置: 各 crate 内部的 tests/ 目录                                   │  │
│  │   范围: 单个函数/模块的功能验证                                       │  │
│  │                                                                        │  │
│  │   router/tests/     → 路由器单元测试                                  │  │
│  │   ├── adaptor_tests.rs    协议适配测试                                │  │
│  │   ├── auth_tests.rs       认证测试                                    │  │
│  │   ├── balancer_tests.rs   负载均衡测试                                │  │
│  │   ├── billing_tests.rs    计费测试                                    │  │
│  │   └── circuit_tests.rs    熔断器测试                                  │  │
│  │                                                                        │  │
│  │   database/tests/  → 数据库单元测试                                   │  │
│  │   ├── integration_tests.rs    集成测试                                │  │
│  │   └── performance_tests.rs    性能测试                                │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                        集成测试 (Integration Tests)                   │  │
│  │                                                                        │  │
│  │   位置: crates/tests/                                                 │  │
│  │   范围: 多组件协同、完整流程验证                                      │  │
│  │                                                                        │  │
│  │   API 测试: auth.rs, user.rs, channel.rs, relay.rs, ...               │  │
│  │   UI 测试: basic_render.rs, runner.rs                                 │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第五层：测试执行流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              测试执行流程                                    │
└─────────────────────────────────────────────────────────────────────────────┘

    cargo test --all-features
             │
             ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          测试环境初始化                                      │
│                                                                              │
│  1. 创建临时数据库 (SQLite :memory:)                                        │
│  2. 运行数据库迁移                                                          │
│  3. 插入测试数据                                                            │
│  4. 启动测试服务器                                                          │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          测试用例执行                                        │
│                                                                              │
│  API 测试:                                                                   │
│  发送 HTTP 请求 → 验证响应状态 → 检查响应数据 → 清理测试数据               │
│                                                                              │
│  UI 测试:                                                                    │
│  渲染组件 → 查找 DOM 元素 → 触发用户事件 → 验证 UI 状态                    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          测试结果汇总                                        │
│                                                                              │
│  test result: ok. 120 passed; 0 failed; 0 ignored                           │
│                                                                              │
│  • 通过数量 • 失败数量 • 忽略数量 • 执行时间                                │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第六层：运行命令

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              运行命令                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  # 运行所有测试                                                              │
│  cargo test --all-features                                                   │
│                                                                              │
│  # 运行特定集成测试                                                          │
│  cargo test -p burncloud-tests --test api_tests                             │
│  cargo test -p burncloud-tests --test ui_tests                              │
│                                                                              │
│  # 运行特定测试用例                                                          │
│  cargo test -p burncloud-tests test_auth                                    │
│                                                                              │
│  # 运行 router 测试                                                          │
│  cargo test -p burncloud-router                                              │
│                                                                              │
│  # 运行数据库测试                                                            │
│  cargo test -p burncloud-database                                            │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第七层：测试原则

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              测试原则                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  1. 自举测试                                                                 │
│     • 测试自动启动服务、运行测试、停止服务                                  │
│     • 不依赖外部环境                                                        │
│                                                                              │
│  2. 无硬编码密钥                                                             │
│     • 使用环境变量                                                          │
│     • 测试使用临时密钥                                                      │
│                                                                              │
│  3. 路径对应                                                                 │
│     • 测试文件路径对应路由路径                                              │
│     • /v1/chat/completions → tests/v1/chat/completions_test.rs              │
│                                                                              │
│  4. 隔离性                                                                   │
│     • 每个测试独立数据库                                                    │
│     • 测试间不共享状态                                                      │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第七层：文件级详解

### src/lib.rs - 测试库入口

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            src/lib.rs                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  测试库声明:                                                                 │
│  pub mod common;                                                             │
│                                                                              │
│  公开工具函数:                                                               │
│  pub use common::*;                                                          │
│                                                                              │
│  重导出测试工具:                                                             │
│  pub use burncloud_server;                                                   │
│  pub use burncloud_router;                                                   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### tests/api_tests.rs - API 测试入口

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        tests/api_tests.rs                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  集成测试入口:                                                               │
│  mod api;                                                                    │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_all_api() {                                                   │
│      // 运行所有 API 测试                                                   │
│  }                                                                           │
│                                                                              │
│  测试范围:                                                                   │
│  ├── 认证测试 (auth)                                                         │
│  ├── 用户测试 (user)                                                         │
│  ├── 通道测试 (channel)                                                      │
│  ├── Token 测试 (token)                                                      │
│  ├── 中继测试 (relay)                                                        │
│  └── 监控测试 (monitor)                                                      │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### tests/api/mod.rs - API 测试模块

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         tests/api/mod.rs                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  模块声明:                                                                   │
│  pub mod auth;                                                               │
│  pub mod auth_handlers;                                                      │
│  pub mod user;                                                               │
│  pub mod channel;                                                            │
│  pub mod token;                                                              │
│  pub mod relay;                                                              │
│  pub mod log;                                                                │
│  pub mod monitor;                                                            │
│  pub mod status;                                                             │
│  pub mod ability_routing;                                                    │
│  pub mod claude_relay;                                                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### tests/api/auth.rs - 认证测试

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         tests/api/auth.rs                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_login_success() {                                             │
│      // 测试正常登录                                                        │
│      let response = client.post("/api/auth/login")                          │
│          .json(&login_req).send().await;                                    │
│      assert_eq!(response.status(), 200);                                    │
│      assert!(response.json::<LoginResponse>().await.token.len() > 0);       │
│  }                                                                           │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_login_wrong_password() {                                      │
│      // 测试密码错误                                                        │
│  }                                                                           │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_me_with_valid_token() {                                       │
│      // 测试获取当前用户                                                    │
│  }                                                                           │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_me_without_token() {                                          │
│      // 测试无 Token 访问                                                   │
│  }                                                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### tests/api/user.rs - 用户测试

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         tests/api/user.rs                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_list_users() { ... }                                          │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_create_user() { ... }                                         │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_create_duplicate_username() { ... }                           │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_update_user() { ... }                                         │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_delete_user() { ... }                                         │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_update_balance() {                                            │
│      // 测试更新双币种余额                                                  │
│  }                                                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### tests/api/relay.rs - 中继测试

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        tests/api/relay.rs                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_chat_completions() {                                          │
│      // 测试 /v1/chat/completions 端点                                      │
│      // 需要 TEST_OPENAI_API_KEY 环境变量                                   │
│  }                                                                           │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_streaming_response() {                                        │
│      // 测试流式响应                                                        │
│  }                                                                           │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_token_quota() {                                               │
│      // 测试配额检查                                                        │
│  }                                                                           │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_invalid_token() {                                             │
│      // 测试无效 Token                                                      │
│  }                                                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### tests/api/channel.rs - 通道测试

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       tests/api/channel.rs                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_list_channels() { ... }                                       │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_create_channel() { ... }                                      │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_update_channel() { ... }                                      │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_delete_channel() { ... }                                      │
│                                                                              │
│  #[tokio::test]                                                              │
│  async fn test_channel_reload_router() {                                     │
│      // 测试通道变更后路由器重载                                            │
│  }                                                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### tests/ui/mod.rs - UI 测试模块

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         tests/ui/mod.rs                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  模块声明:                                                                   │
│  pub mod runner;                                                             │
│  pub mod basic_render;                                                       │
│                                                                              │
│  UI 测试类型:                                                                │
│  ├── 组件渲染测试                                                            │
│  ├── 交互测试                                                                │
│  └── 路由测试                                                                │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### tests/ui/runner.rs - UI 测试运行器

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        tests/ui/runner.rs                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  pub struct UiTestRunner {                                                   │
│      // 测试运行器状态                                                       │
│  }                                                                           │
│                                                                              │
│  impl UiTestRunner {                                                         │
│      pub fn new() -> Self;                                                   │
│      pub fn render<T: Component>(&self) -> String;                          │
│      pub fn find(&self, selector: &str) -> Option<Element>;                 │
│      pub fn click(&self, selector: &str) -> Result<()>;                     │
│  }                                                                           │
│                                                                              │
│  测试能力:                                                                   │
│  ├── 虚拟 DOM 渲染                                                           │
│  ├── 元素查找                                                                │
│  └── 事件模拟                                                                │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### tests/ui/basic_render.rs - 基础渲染测试

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     tests/ui/basic_render.rs                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  #[test]                                                                     │
│  fn test_home_renders() {                                                    │
│      // 测试首页渲染                                                        │
│  }                                                                           │
│                                                                              │
│  #[test]                                                                     │
│  fn test_login_form_renders() {                                              │
│      // 测试登录表单渲染                                                    │
│  }                                                                           │
│                                                                              │
│  #[test]                                                                     │
│  fn test_layout_structure() {                                                │
│      // 测试布局结构                                                        │
│  }                                                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### tests/common/mod.rs - 公共测试工具

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       tests/common/mod.rs                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  pub async fn setup_test_db() -> Database {                                  │
│      // 创建内存数据库                                                      │
│      // 运行迁移                                                            │
│      // 插入测试数据                                                        │
│  }                                                                           │
│                                                                              │
│  pub async fn setup_test_server(db: Arc<Database>) -> TestServer {          │
│      // 启动测试服务器                                                      │
│      // 返回服务器地址                                                      │
│  }                                                                           │
│                                                                              │
│  pub fn create_test_user(db: &Database) -> User {                           │
│      // 创建测试用户                                                        │
│      // 用户名: testuser                                                     │
│      // 密码: testpass                                                       │
│  }                                                                           │
│                                                                              │
│  pub fn create_test_token(db: &Database, user_id: &str) -> Token {          │
│      // 创建测试 Token                                                      │
│  }                                                                           │
│                                                                              │
│  pub async fn cleanup_test_db(db: &Database) {                              │
│      // 清理测试数据                                                        │
│  }                                                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### tests/pricing_u64_test.rs - 价格精度测试

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    tests/pricing_u64_test.rs                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  #[test]                                                                     │
│  fn test_dollars_to_nano() {                                                 │
│      assert_eq!(dollars_to_nano(1.0), 1_000_000_000);                       │
│      assert_eq!(dollars_to_nano(0.002), 2_000_000);                         │
│      assert_eq!(dollars_to_nano(0.000001), 1_000);                          │
│  }                                                                           │
│                                                                              │
│  #[test]                                                                     │
│  fn test_nano_to_dollars() { ... }                                           │
│                                                                              │
│  #[test]                                                                     │
│  fn test_rate_conversion() { ... }                                           │
│                                                                              │
│  #[test]                                                                     │
│  fn test_calculate_cost() { ... }                                            │
│                                                                              │
│  #[test]                                                                     │
│  fn test_precision_no_loss() {                                               │
│      // 验证 i64 精度无丢失                                                 │
│  }                                                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 依赖关系

```
burncloud-tests
├── burncloud-server       # 测试服务器
├── burncloud-router       # 路由器测试
├── burncloud-database     # 数据库测试
├── burncloud-common       # 共享类型
└── external
    ├── tokio-test         # 异步测试
    └── reqwest           # HTTP 客户端
```
