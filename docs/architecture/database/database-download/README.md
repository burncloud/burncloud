# database-download

下载数据访问层，管理下载任务的状态和进度。

## 🧅 第一层：在 Database 中的位置

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          burncloud-database                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌────────────────────────────────────────────────────────────────────┐    │
│   │                        数据库层                                     │    │
│   │                                                                     │    │
│   │   ┌────────────────┐  ┌────────────────┐  ┌────────────────────┐   │    │
│   │   │database-router │  │ database-user  │  │★database-download  │   │    │
│   │   │                │  │                │  │                    │   │    │
│   │   │ Token 验证     │  │ 用户 CRUD      │  │ 下载任务管理       │   │    │
│   │   │ 配额管理       │  │ 余额操作       │  │ 进度追踪           │   │    │
│   │   │ 日志存储       │  │                │  │ 状态持久化         │   │    │
│   │   └────────────────┘  └────────────────┘  └────────────────────┘   │    │
│   │                                                                     │    │
│   └────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

database-download 的职责:
├── 下载任务创建和删除
├── 下载进度更新
├── 下载状态管理
└── 任务列表查询
```

## 🧅 第二层：文件结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       database-download 文件结构                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  crates/database/crates/database-download/                                   │
│  │                                                                           │
│  ├── Cargo.toml           # 依赖配置                                        │
│  └── src/                                                                    │
│      └── lib.rs           # 下载数据访问实现                                │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第三层：lib.rs 详解

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              lib.rs                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  模块职责: 下载任务的状态持久化                                              │
│                                                                              │
│  操作的数据表: downloads                                                     │
│                                                                              │
│  表结构:                                                                     │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  downloads 表                                                         │   │
│  │                                                                      │   │
│  │  gid               TEXT PRIMARY KEY   aria2 任务 ID                  │   │
│  │  status            TEXT               状态 (waiting/downloading/...) │   │
│  │  uris              TEXT               下载链接 JSON 数组             │   │
│  │  total_length      INTEGER            文件总大小 (字节)              │   │
│  │  completed_length  INTEGER            已下载大小 (字节)              │   │
│  │  download_speed    INTEGER            下载速度 (字节/秒)             │   │
│  │  download_dir      TEXT               保存目录                       │   │
│  │  filename          TEXT               文件名                         │   │
│  │  connections       INTEGER            连接数 (默认 16)               │   │
│  │  split             INTEGER            分片数 (默认 5)                │   │
│  │  created_at        DATETIME           创建时间                       │   │
│  │  updated_at        DATETIME           更新时间                       │   │
│  │                                                                      │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第四层：数据结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          数据结构定义                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  /// 下载任务记录                                                            │
│  pub struct Download {                                                       │
│      pub gid: String,              // aria2 任务全局 ID                      │
│      pub status: String,           // 状态: waiting, downloading, complete  │
│      pub uris: String,             // 下载链接 JSON 数组                     │
│      pub total_length: i64,        // 文件总大小 (字节)                      │
│      pub completed_length: i64,    // 已下载大小 (字节)                      │
│      pub download_speed: i64,      // 下载速度 (字节/秒)                     │
│      pub download_dir: Option<String>,  // 保存目录                         │
│      pub filename: Option<String>,      // 文件名                           │
│      pub connections: i32,         // 连接数                                │
│      pub split: i32,               // 分片数                                │
│      pub created_at: String,       // 创建时间                              │
│      pub updated_at: String,       // 更新时间                              │
│  }                                                                           │
│                                                                              │
│  /// 下载数据库管理器                                                        │
│  pub struct DownloadDB {                                                     │
│      db: Database,                 // 底层数据库连接                        │
│  }                                                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第五层：核心函数

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          核心函数                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  /// 创建新实例 (自动初始化表)                                               │
│  pub async fn new() -> Result<Self>                                          │
│                                                                              │
│  /// 添加下载任务                                                            │
│  pub async fn add(                                                           │
│      &self,                                                                  │
│      gid: &str,                    // aria2 任务 ID                         │
│      uris: Vec<String>,            // 下载链接列表                          │
│      download_dir: Option<&str>,   // 保存目录                              │
│      filename: Option<&str>,       // 文件名                                │
│  ) -> Result<()>                                                             │
│                                                                              │
│  /// 更新下载进度                                                            │
│  pub async fn update_progress(                                               │
│      &self,                                                                  │
│      gid: &str,                                                              │
│      total: i64,                   // 文件总大小                            │
│      completed: i64,               // 已下载大小                            │
│      speed: i64,                   // 下载速度                              │
│  ) -> Result<()>                                                             │
│                                                                              │
│  /// 更新下载状态                                                            │
│  pub async fn update_status(&self, gid: &str, status: &str) -> Result<()>    │
│                                                                              │
│  /// 更新任务 GID (任务重启时)                                               │
│  pub async fn update_gid(&self, old_gid: &str, new_gid: &str) -> Result<()>  │
│                                                                              │
│  /// 获取下载任务                                                            │
│  pub async fn get(&self, gid: &str) -> Result<Option<Download>>              │
│                                                                              │
│  /// 列出下载任务 (可按状态筛选)                                             │
│  pub async fn list(&self, status: Option<&str>) -> Result<Vec<Download>>     │
│                                                                              │
│  /// 删除下载任务                                                            │
│  pub async fn delete(&self, gid: &str) -> Result<()>                         │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第六层：下载状态流转

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          下载状态流转                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────┐     ┌─────────────┐     ┌──────────┐     ┌───────────┐       │
│   │ waiting │ ──► │ downloading │ ──► │ complete │     │  error    │       │
│   └─────────┘     └─────────────┘     └──────────┘     └───────────┘       │
│        │                 │                                                   │
│        │                 ├──────────────────────►│                          │
│        │                 │                      ▼                           │
│        │                 │               ┌───────────┐                      │
│        │                 └──────────────►│  paused   │                      │
│        │                                 └───────────┘                      │
│        │                                      │                             │
│        └──────────────────────────────────────┘                             │
│                                                                              │
│   状态说明:                                                                  │
│   ├── waiting:      等待下载                                                │
│   ├── downloading:  正在下载                                                │
│   ├── complete:     下载完成                                                │
│   ├── error:        下载失败                                                │
│   └── paused:       已暂停                                                  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 第七层：与 aria2 的交互

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      与 aria2 的交互流程                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌───────────────┐                      ┌───────────────┐                  │
│   │   BurnCloud   │                      │    aria2      │                  │
│   │   应用层      │                      │   下载引擎    │                  │
│   └───────┬───────┘                      └───────┬───────┘                  │
│           │                                      │                          │
│           │  1. 添加下载任务                      │                          │
│           │ ──────────────────────────────────► │                          │
│           │                                      │                          │
│           │  2. 返回 gid                         │                          │
│           │ ◄────────────────────────────────── │                          │
│           │                                      │                          │
│           │  3. 保存 gid 到 database-download   │                          │
│           │                                      │                          │
│           │  4. 轮询进度                         │                          │
│           │ ──────────────────────────────────► │                          │
│           │                                      │                          │
│           │  5. 返回进度信息                     │                          │
│           │ ◄────────────────────────────────── │                          │
│           │                                      │                          │
│           │  6. 更新 database-download 进度     │                          │
│           │                                      │                          │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🧅 依赖关系

```
database-download
├── burncloud-database
│   └── Database (连接池)
└── external
    ├── sqlx            # 数据库访问
    └── serde           # 序列化
```
